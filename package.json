{
  "name": "microfrontend-architecture",
  "version": "1.0.0",
  "description": "microfrontend-architecture with lerna monorepo",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "lerna run --scope @demo/* --parallel start",
    "build": "lerna run --scope @demo/* build",
    "serve": "lerna run --scope @demo/* --parallel serve",
    "clean": "lerna run --scope @demo/* --parallel clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
